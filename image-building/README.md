# Making HumbleBundleScraper Container Images

This directory contains all that you need to create container images for the
scraper component.


To build a docker image, invoke the script: `build-image.sh`
